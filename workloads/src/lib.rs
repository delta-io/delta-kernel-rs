//! Shared workload specification types and predicate parsing for Delta Kernel
//! correctness testing (`acceptance`) and benchmarking (`delta_kernel_benchmarks`).

pub mod models;
pub mod predicate_parser;
